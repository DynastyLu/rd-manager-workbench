import { useState } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog'
import { Input } from '@/components/ui/input'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { createRisk, listRisks } from '@/modules/workbench/api/management'
import { ManagementEmpty, ManagementError, ManagementLoading } from '@/modules/workbench/components/management/ManagementState'
import type { RiskStatus } from '@/modules/workbench/types'
export default function RisksPage(){const[status,setStatus]=useState<RiskStatus|undefined>();const[open,setOpen]=useState(false);const q=useQuery({queryKey:['risks',{status}],queryFn:()=>listRisks({status})});const qc=useQueryClient();const m=useMutation({mutationFn:(form:HTMLFormElement)=>{const data=new FormData(form);return createRisk({title:data.get('title') as string,likelihood:'HIGH',impact:'HIGH',level:'HIGH',ownerName:(data.get('ownerName') as string)||undefined})},onSuccess:()=>{void qc.invalidateQueries({queryKey:['risks']});void qc.invalidateQueries({queryKey:['dashboard']});void qc.invalidateQueries({queryKey:['projects']});setOpen(false)}});return <main className="app-page"><div className="app-page__inner app-page__inner--wide"><header className="app-page__hero"><div><p className="app-page__eyebrow">Management Loop</p><h1 className="app-page__title">风险对象库</h1><p className="app-page__subtitle">按状态筛选未关闭风险；高风险会实时影响关联项目健康度。</p></div><Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button>新建风险</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>新建风险</DialogTitle></DialogHeader><form className="grid gap-3" onSubmit={e=>{e.preventDefault();m.mutate(e.currentTarget)}}><Input name="title" required placeholder="风险标题"/><Input name="ownerName" placeholder="责任人"/><Button disabled={m.isPending}>保存风险</Button></form></DialogContent></Dialog></header><Card className="mb-4"><CardContent className="pt-4"><Select value={status??'ALL'} onValueChange={v=>setStatus(v==='ALL'?undefined:v as RiskStatus)}><SelectTrigger aria-label="按风险状态筛选"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="ALL">全部状态</SelectItem><SelectItem value="OPEN">未关闭</SelectItem><SelectItem value="MITIGATING">处理中</SelectItem><SelectItem value="CLOSED">已关闭</SelectItem></SelectContent></Select></CardContent></Card>{q.isPending?<ManagementLoading label="风险"/>:null}{q.isError?<ManagementError label="风险" retry={()=>void q.refetch()}/>:null}{q.data?(q.data.data.length?<section className="grid gap-3">{q.data.data.map(r=><Card key={r.id}><CardHeader><CardTitle>{r.title}</CardTitle></CardHeader><CardContent className="text-sm text-muted-foreground">等级：{r.level} · 状态：{r.status} · 负责人：{r.ownerName??'未指定'}<p className="mt-2">详情可继续关联任务、项目和处置记录。</p></CardContent></Card>)}</section>:<ManagementEmpty label="风险"/>):null}</div></main>}
