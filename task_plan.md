# 任务计划：研发主管本地工作台

> **当前工程基线（2026-07-18）：** 旧根 pnpm workspace、`apps/` 与 `packages/` 已由直接工程迁移替代。当前开发仅在独立的 `frontend/` 与 `backend/` 中继续；旧 workspace 记录保留在文档中作为历史，不是当前执行入口。

## 直接工程迁移状态

| Task | 状态 | 当前工程落点 |
|------|------|--------------|
| Task 1：建立可追溯的真实工程副本 | complete | `frontend/`、`backend/`、`docs/migration/` |
| Task 2：用真实前端框架替换旧业务 | complete | `frontend/` |
| Task 3：用真实后端框架替换旧业务并接入本地 PostgreSQL | complete | `backend/` |
| Task 4：移除错误自建结构并验证独立运行 | complete | 根说明与忽略规则；运行命令仍位于 `frontend/`、`backend/` |

## 目标
基于 treasure-box 与 backend-core-platform 的工程能力，设计并实施一个 Electron 单机桌面应用，使用本机 PostgreSQL，完整覆盖需求文档并按 MVP 分阶段交付。

## 当前阶段
阶段 3：真实前后端直接迁移已完成；后续工作在 `frontend/` 与 `backend/` 中推进。

## 各阶段

### 阶段 1：需求设计与范围确认
- [x] 完整读取并渲染需求文档
- [x] 确认第一版为单机、本地自用
- [x] 确认桌面外壳采用 Electron
- [x] 确认数据库采用本机 PostgreSQL 并新建数据库
- [x] 梳理完整功能域、业务规则、数据关系和 MVP 边界
- [x] 获得用户对完整设计的确认
- **状态：** complete

### 阶段 2：设计规格与实施计划
- [x] 编写并自检设计规格
- [x] 用户确认设计内容并授权开始实施；书面规格与已确认内容保持一致
- [x] 编写可执行的实施计划
- **状态：** complete

### 阶段 3：新工作区与基座提纯
- [x] 直接迁入 treasure-box 前端工程并删除旧业务（`frontend/`）
- [x] 直接迁入 backend-core-platform 后端工程并删除旧业务（`backend/`）
- [x] 删除旧根 workspace、`apps/` 与 `packages/` 结构
- [x] 验证前后端可从各自目录独立运行
- **状态：** complete

### 阶段 4：MVP 功能实现
- [ ] 子项目 1：项目执行 P0（项目、里程碑、任务、进展、首页驾驶舱）
- [ ] 子项目 2：申报认定 P0
- [ ] 子项目 3：管理闭环 P0（风险、问题、决策、合作与会议）
- [ ] 子项目 4：行业情报 P0
- [ ] 子项目 5：数据治理 P0（附件、提醒、搜索、导入导出、备份恢复、审计）
- [ ] 子项目 6：P1 与 P2 扩展
- [ ] Electron 桌面外壳与打包
- [ ] 为核心业务规则补充自动化测试
- [ ] 持续记录数据迁移和兼容性决策
- **状态：** in_progress（当前：子项目 1 的实施计划已就绪）

### 阶段 5：验证与交付
- [ ] 验证功能、数据、备份恢复和桌面打包
- [ ] 运行完整质量门禁
- [ ] 检查安装与首次启动流程
- [ ] 完成交付说明
- **状态：** pending

## 关键问题
1. 新工作区采用单仓库还是保留前后端独立仓库并增加打包编排层？
2. 本机 PostgreSQL 的连接信息、数据库命名和首次初始化方式如何约定？
3. 全量需求中哪些模块进入首次可运行版本，哪些只完成数据与扩展接口？

## 已做决策
| 决策 | 理由 |
|------|------|
| 创建新工作区，原始两个仓库不直接清理 | 两个源仓库均存在大量未提交改动，避免误删用户成果 |
| Electron 作为桌面外壳 | 与 React、NestJS、Node.js 技术栈兼容度最高 |
| 第一版单机本地使用 | 用户已明确，无需第一期实现局域网多人协作 |
| 使用本机 PostgreSQL 并新建数据库 | 用户明确指定，不采用 SQLite |
| 先完成设计再实施 | 项目范围大，需要先固定模块边界和验收标准 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| DOCX 在 LibreOffice 渲染中中文字体显示方框 | 1 | 使用 OOXML/文档结构完整提取文字，并结合页图核对版式 |
| 设计规格补丁上下文因分段输出边界重复而未匹配 | 1 | 缩小补丁上下文，仅修改实际存在的目标行 |
| 合并准备代理发现时补丁包含已变化上下文 | 1 | 使用 `rg` 定位实际内容后分段应用补丁 |
| `git check-ignore .worktrees` 在目录不存在时返回未匹配 | 1 | 使用 `git check-ignore --no-index .worktrees/placeholder` 验证目录规则 |

## 备注
- 原始基座：`/Users/dynastylu/Desktop/AICode/treasure-box`
- 原始基座：`/Users/dynastylu/Desktop/AICode/backend-core-platform`
- 原始仓库只读保留，实施时复制所需框架能力到新工作区。
